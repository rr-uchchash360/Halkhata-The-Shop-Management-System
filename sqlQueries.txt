ALTER TABLE products(
ADD COLUMN buying_price INT,
);

CREATE TABLE orders(
order_id VARCHAR(20),
customer_contact VARCHAR(20),
product_id VARCHAR(20), 
product_quantity INT, 
total_price INT
PRIMARY KEY(order_id)
);